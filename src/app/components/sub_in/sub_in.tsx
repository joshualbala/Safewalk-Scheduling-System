'use client'
import React, { useState, useRef, useEffect } from "react";
import {InProps} from "@/app/components/sub_in/InProps"
export const Sub_In: React.FC<InProps> = (props) => {
    return(
        <>
        </>
    )

}